# features we might add:



mvp:

make sure when user clicks send over and over on an image, that we only store it once.  like they can send the same image multiple times, but we only store it once and point to the same file.





someday: